# Clientquerymanagementsystem
client query management system creates a Streamlit-based Customer Helpdesk system that allows users to log in, submit queries, and store them in a PostgreSQL database. Admin/support users can view live DB records, compare them with old CSV history, and close queries with timestamps. 
